let initialState = {
    friends: [
        { id: 1, name: "Vasya" },
        { id: 2, name: "Sasha" },
        { id: 3, name: "Sveta" },
    ]
};

const navbarReducer = (state = initialState, action) => {
    return state;
}

export default navbarReducer;